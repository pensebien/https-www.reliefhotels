/**
 * @deprecated Use `ReservationForm` from `@/features/reservations` — two-part flow
 * (Part 1: guest details → Part 2: pay 20% deposit) is required for Paystack init.
 */
export { ReservationForm as BookingForm } from "@/features/reservations";
